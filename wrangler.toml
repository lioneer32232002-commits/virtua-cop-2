name = "virtua-cop-2"
compatibility_date = "2024-10-01"

# Serve the Unity WebGL build output as a Workers static-assets site.
# `wrangler deploy` reads this file; CI sets the build dir before deploying.
[assets]
directory = "./build/WebGL/VirtuaCop2"
not_found_handling = "single-page-application"
